!!!
%html

  %head
    %title uludum, a world school
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}
    %meta{name: "viewport", content: "width=device-width, initial-scale=1.0"}
    = stylesheet_link_tag "http://fonts.googleapis.com/css?family=Junge","http://fonts.googleapis.com/css?family=Lato:300,400,700", "application", "/assets/bootstrap-wysihtml5/index.css", "/assets/bootstrap-wysihtml5/core.css"
    = javascript_include_tag "//api.filepicker.io/v1/filepicker.js", "application"
    = csrf_meta_tag
    = favicon_link_tag
    = yield(:head)

  %body
    .container.page-container
      .navbar
        .navbar-inner
          .container{style: "width: auto; padding: 0 20px;"}
            %a.brand{href: courses_path} uludum
            %ul.nav
              - if current_user
                %li
                  - username_link = current_user.username + " (#{current_user.points})"
                  - username_link = avatar_tag(current_user, height: 18, class: 'avatar-small') + username_link unless current_user.avatar_url.blank?
                  - username_link += "<b class='caret'></b>".html_safe
                  %li.dropdown
                    = link_to username_link, user_path(current_user), class: 'dropdown-toggle', 'data-toggle' => 'dropdown'
                    %ul.dropdown-menu{role: 'menu'}
                      %li= link_to "My Courses", courses_path(enrolled: true)
                      %li= link_to "My Taught Courses", courses_path(taught: true)
                      %li.divider
                      %li= link_to "View Profile", user_path(current_user)
                      %li= link_to "Edit Profile", edit_user_path(current_user)
                      %li= link_to "Sign Out", logout_path
              - else
                %li= link_to "Log In", new_session_path(return_to: request.fullpath)
                %li= link_to "Sign Up", signup_path
              - unless current_course.nil? || current_course.title.nil? || enrolled?(current_course) || !logged_in?
                %li= link_to "Enroll in #{current_course.title}", new_enrollment_path(course_id: current_course.id)
            / - #if logged_in? && enrolled?(current_course)
            /   - p = percent_complete
            /   - unless p.nil?
            /     .progress-area.pull-right
            /       %span.progress-display= p.to_s + "%"
            /       .progress.progress-success
            /         .bar{style: "width: #{p}%;"}

            = form_tag courses_path, class: 'navbar-search pull-right', method: 'get' do
              = text_field_tag :search, params[:search], placeholder: "What do you want to learn?"
              = submit_tag "Search", class: 'btn'

      #container

        .row.main
          - row_size = "12"
          - if content_for? :sidebar
            .span3#sidebar
              %ul.nav.nav-list
                - if current_course && enrolled?(current_course)
                  = progress
                = yield :sidebar
            - row_size = "9"
          .content{class: "span#{row_size}"}
            %span#notice= notice
            - if flash[:alert]
              .alert
                = flash[:alert]
            = yield
      .break
      %ul#footer.nav-pills
        %li= link_to "About", "#"
        %li= link_to "Create a Course", new_course_path
        %li © Uludum Learning Organization
